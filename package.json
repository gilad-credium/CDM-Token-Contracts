{
  "name": "credium-contracts",
  "version": "0.0.1",
  "author": "Taras Herasymchuk",
  "repository": {
    "type": "git"
  },
  "engines": {
    "npm": "^8.9.4"
  },
  "scripts": {
    "testnet": "ganache-cli",
    "test": "solium -d solidity/ && cd solidity && truffle test",
    "deploy": "cd solidity && truffle compile && truffle migrate --reset",
    "dev-console": "cd solidity && truffle console"
  },
  "dependencies": {
    "ganache-cli": "^6.0.3",
    "solium": "^1.1.5",
    "truffle": "^4.0.5"
  },
  "devDependencies": {
    "mocha": "^5.0.1",
    "chai": "^4.1.2"
  }
}
